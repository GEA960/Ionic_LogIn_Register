import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic_LogIn_Register',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
